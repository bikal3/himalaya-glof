# Runtime dependencies for the Streamlit app.
# Pinned with ~= (compatible release: patch updates only) so a deployment cannot pick up a
# breaking minor release at build time — an unpinned `use_container_width` removal, for
# instance, would take the live app down without any code change here.
# Offline data-preparation dependencies live in requirements-offline.txt.
streamlit~=1.57.0
streamlit-folium~=0.27.2
folium~=0.20.0
pandas~=3.0.2
geopandas~=1.1.3
shapely~=2.1.2
numpy~=2.4.4
plotly~=6.7.0
scikit-learn~=1.8.0
joblib~=1.5.3
fpdf2~=2.8.7
