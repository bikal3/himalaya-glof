# Dependencies for building the static site (site/build.py) and running the tests.
# Pinned with ~= (compatible release: patch updates only) so a build cannot pick up a
# breaking minor release. Offline data-preparation dependencies live in
# requirements-offline.txt.
#
# The published site needs no Python at runtime — it is static files on Cloudflare Pages.
# Leaflet and Plotly ship as vendored JavaScript under site/vendor/.
pandas~=3.0.2
geopandas~=1.1.3
shapely~=2.1.2
numpy~=2.4.4
scikit-learn~=1.8.0
joblib~=1.5.3
fpdf2~=2.8.7
